# ── Database ──────────────────────────────────────────────────────────
# For Docker: the compose file builds DATABASE_URL from these.
# For local dev without Docker: set DATABASE_URL directly.
# IMPORTANT: Change POSTGRES_PASSWORD to a strong, unique value before any deployment.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_RANDOM_PASSWORD
POSTGRES_DB=ai_judge
# DATABASE_URL=postgresql+asyncpg://postgres:YOUR_PASSWORD_HERE@localhost:5432/ai_judge

# ── LLM API Keys (required for AI features) ─────────────────────────
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# ── Application ──────────────────────────────────────────────────────
DEBUG=true
CORS_ORIGINS=["http://localhost:3000","http://localhost"]

# ── Auth / Roles ─────────────────────────────────────────────────────
ADMIN_API_KEYS=["replace-with-strong-admin-key"]

# ── Field-level Encryption (PII protection) ──────────────────────────
# Required for encrypting names, addresses, phone numbers at rest.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FIELD_ENCRYPTION_KEY=
